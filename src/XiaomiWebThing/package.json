{
  "name": "XiaomiWebthing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "x": "nodemon --exec \"ts-node --cache-directory .tscache\" ./src/index.ts",
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.10.3",
    "nodemon": "^1.18.4",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  },
  "dependencies": {}
}
